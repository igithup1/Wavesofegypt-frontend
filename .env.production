# Production API URL — baked into the bundle by Vite at build time.
# This file is committed so Vercel picks it up without manual env var configuration.
VITE_API_URL=https://wavesofegypt.replit.app/api
