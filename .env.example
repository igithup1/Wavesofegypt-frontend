# URL of the deployed API server (baked into the bundle at build time by Vite).
# Already set correctly in .env.production — no manual configuration needed.
VITE_API_URL=https://wavesofegypt.replit.app/api
